require 'spec_helper'

describe AnsibleSkeleton do
  it 'has a version number' do
    expect(AnsibleSkeleton::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
